# Delhi-Metro-Route-Simulator
A Python-based simulation of the Delhi Metro (Blue &amp; Magenta Lines) that calculates optimal routes and train schedules without using external libraries. Implements state-space search logic for route planning
